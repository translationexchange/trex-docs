<ul class="tx-inline">
    {{#foreach navigation}}
        <li class="{{slug}}{{#if current}} tx-nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
    {{/foreach}}
</ul>
